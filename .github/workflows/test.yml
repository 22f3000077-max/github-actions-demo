name: Test Workflow

on:
  workflow_dispatch:   # allows manual trigger from Actions UI
  push:                # also triggers on pushes to the default branch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 22f3000077@ds.study.iitm.ac.in
        run: echo "Hello, world!"
